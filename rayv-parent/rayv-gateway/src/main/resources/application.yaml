
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true

server:
  port: 1011

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/